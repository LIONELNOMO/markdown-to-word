# Normalise les fins de ligne dans le dépôt (LF), quel que soit l'OS de travail.
* text=auto

*.png binary
*.docx binary

# Les scripts de commandes Windows exigent des fins de ligne CRLF.
*.cmd text eol=crlf
*.bat text eol=crlf
